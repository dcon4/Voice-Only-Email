<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
        Required to run VoiceWakeService as a foreground service.
        FOREGROUND_SERVICE_SPECIAL_USE is needed for apps targeting API 34+
        that use a foreground service whose purpose (screen-on monitoring for
        accessibility) does not map to a more specific foreground service type.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!--
        On Android 13+ the user must grant notification permission for the
        foreground service notification to appear in the status bar.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".VoiceGmailApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceGmail">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                OAuth redirect intent-filter on MainActivity (singleTask) so that:
                  - When the process is alive, onNewIntent() fires on the existing instance.
                  - When the process was killed while Chrome was open, Android starts a fresh
                    MainActivity and calls onCreate() with the redirect URI in intent.data.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${appAuthRedirectScheme}" />
            </intent-filter>
        </activity>

        <!--
            Disable AppAuth's built-in RedirectUriReceiverActivity so it cannot
            compete with MainActivity for the OAuth redirect URI.
        -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true"
            tools:node="replace" />

        <!--
            Foreground service that monitors ACTION_SCREEN_ON and brings
            MainActivity to the front, enabling a totally blind user to start
            giving voice commands simply by pressing the power button.
        -->
        <service
            android:name=".VoiceWakeService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Accessibility wake service — surfaces VoiceGmail over the lock screen when the power button is pressed, enabling hands-free voice control for blind users without requiring screen unlock." />
        </service>

    </application>

</manifest>
